const navToggle=document.querySelector(".nav-toggle");const siteNav=document.querySelector(".site-nav");if(navToggle&&siteNav){navToggle.addEventListener("click",()=>{const e=siteNav.classList.toggle("open");navToggle.setAttribute("aria-expanded",String(e))});siteNav.querySelectorAll("a").forEach(e=>{e.addEventListener("click",()=>{siteNav.classList.remove("open");navToggle.setAttribute("aria-expanded","false")})})}const revealItems=document.querySelectorAll(".reveal");const revealOnScroll=new IntersectionObserver(e=>{e.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");revealOnScroll.unobserve(e.target)}})},{threshold:.12});revealItems.forEach(e=>revealOnScroll.observe(e));